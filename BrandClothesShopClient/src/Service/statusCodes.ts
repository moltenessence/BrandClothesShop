
export enum RegisterCodes {
    Success = 200,
    AlreadyExist = 422,
}

export enum LoginCodes {
    Success = 200,
    InvalidData = 400,
}

export enum OrderCodes {
    Success = 200,
    Error = 400,
}